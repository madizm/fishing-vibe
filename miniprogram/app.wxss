/* 全局样式 */
page {
  font-family: -apple-system, "PingFang SC", "Helvetica Neue", sans-serif;
  background: #f4f7f6;
  color: #14231f;
  box-sizing: border-box;
}

/* 通用变量用 class 替代（小程序不支持 :root CSS 变量在部分基础库版本） */
